{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm run build && npm run prisma:generate"
  },
  "deploy": {
    "startCommand": "cd backend && PORT=3000 npm start"
  }
}
